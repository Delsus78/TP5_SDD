#include "file.c"

tri(Patient) {

}

int main(int argc, char const *argv[])
{
    // init Patient
    Patient p1 = malloc(sizeof(Patient));
    Patient p2 = malloc(sizeof(Patient));
    Patient p3 = malloc(sizeof(Patient));
    Patient p4 = malloc(sizeof(Patient));
    Patient p5 = malloc(sizeof(Patient));
    
    // init valeurs temps


    return 0;
}